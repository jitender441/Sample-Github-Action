name: "CI Pipeline"

on: 
 push:
  branches:
  - master
   
jobs:
 build-and-test:
  name: Build and test
  runs-on: ubuntu-latest   
  steps: 
  - name: "Print hello world"   
    run: echo "Hello world"  
  - name: "Print current date"
    run: date      
  
  